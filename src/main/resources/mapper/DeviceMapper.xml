<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jhun.backend.mapper.DeviceMapper">

    <!-- 按设备编号查询设备，保护全局唯一的 device_number 约束。 -->
    <select id="findByDeviceNumber" resultType="com.jhun.backend.entity.Device">
        SELECT id, name, device_number, category_id, status, approval_mode_override, image_url, description, purchase_date, location,
               status_change_reason, created_at, updated_at
        FROM device
        WHERE device_number = #{deviceNumber}
        LIMIT 1
    </select>

    <!-- 查询未删除设备，并支持按分类筛选。软删除设备状态为 DELETED，不能继续出现在正常列表。 -->
    <select id="findActiveDevices" resultType="com.jhun.backend.entity.Device">
        SELECT id, name, device_number, category_id, status, approval_mode_override, image_url, description, purchase_date, location,
               status_change_reason, created_at, updated_at
        FROM device
        WHERE status != 'DELETED'
          <if test="categoryId != null and categoryId != ''">
              AND category_id = #{categoryId}
          </if>
        ORDER BY created_at DESC
    </select>

    <!--
        仅当设备当前状态与预期状态一致时才允许切换状态，
        用受影响行数作为 CAS 结果，避免并发借出/归还时重复写状态日志或覆盖其他事务的状态流转。
    -->
    <update id="updateStatusIfCurrent">
        UPDATE device
        SET status = #{newStatus},
            status_change_reason = #{reason},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{deviceId}
          AND status = #{expectedStatus}
    </update>
</mapper>
